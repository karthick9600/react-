import React,  {useState} from  "react";
import login from './Login';
function  Login (){

    const [pwd1,setPwd1]=useState("");
    const[pwd2,setPwd2] =useState("");
    const[same,setSame]=useState("false");

    function handledPwd1Change(event){
      setPwd1(event.target.value)// see  in console step of this procedure.
        console.log(event.target.value);
    }
    function handledPwd2Change(event){
      setPwd2(event.target.value)
      
    }
    function handledPasswordCheck(event){
     
      if(pwd1==pwd2){
        console.log("Same");
        setSame(true);
      }
      else{
        console.log("Not Same")
        setSame(flase);
      }
    }


  return (
    <>
      <form className="my-5" style={{ width: "50%", margin: "auto" }}>
        <div className="mb-3">
          <label className="form-label">Email address</label>
          <input type="email" className="form-control" />
        </div>
        
        <div className="mb-3">
          <label className="form-label">Password</label>
          <input
           value={pwd1}
           onChange={handledPwd1Change}
            type="password"
            className="form-control"
            id="exampleInputPassword1"
          />
        </div>
        
        <div className="mb-3">
          <label className="form-label"> Re-enter Password</label>
          <input
           value={pwd2}
           onChange={handledPwd2Change}
            type="password"
            className="form-control"
            id="exampleInputPassword1"
          />
        </div>
        <div className="mb-3 form-check">
          <input type="checkbox"   onChange={handledPasswordCheck}  className="form-check-input" />
          <label className="form-check-label" >
            I Agree
          </label>
        </div>
        {same && <p>Password Match</p>}
        {!same && <p>Password Don't Match</p>}
        <button type="submit" className="btn btn-primary">
          Create Account
        </button>
      </form>
    </>
  );
}
export default Login;